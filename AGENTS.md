# 经管之星开发规则

本文件适用于整个仓库。`frontend/AGENTS.md` 和 `backend/AGENTS.md` 可以增加更具体的规则；发生冲突时，距离目标文件更近的规则优先，但不得违反安全与产品范围。

## 1. 开发前阅读

开始任务前至少阅读：

1. `docs/00-shared/product-scope.md`
2. 与任务对应的 `frontend/README.md` 或 `backend/README.md`。
3. `backend/docs/api/openapi.yaml`
4. 涉及数据时阅读指标口径、数据字典和数据库设计。
5. 涉及 Text2SQL 时阅读 Text2SQL 设计和 SQL 安全策略。
6. `docs/01-project-management/backlog.md`

## 2. 范围规则

- 不实现登录、用户、角色、权限、驾驶舱和不可达台账报表。
- 以 `docs/01-project-management/backlog.md` 的优先级和依赖关系为实施顺序。
- 不得为了“顺手优化”修改无关模块。
- 需求、API、数据模型或安全策略变化时，先更新对应文档或决策记录。

## 2.1 目录所有权

- 前端源代码、前端测试、前端构建配置和前端专属文档只能放在 `frontend/`。
- 后端源代码、后端测试、迁移、Seed、评测集、后端部署文件和后端专属文档只能放在 `backend/`。
- 根目录 `docs/` 只保存前后端共同依赖的产品基线、验收、架构和项目管理文档。
- `des.md.txt` 是原始任务说明，不是正式业务实现；历史演示资料只允许维护在 `docs/DEMONSTRATION.md`。
- 根目录只保留仓库级文件和最终的跨服务编排文件，不在根目录新增前端或后端业务代码。

## 3. 架构规则

- 前后端以 OpenAPI 为唯一接口契约，不允许分别猜测字段。
- 后端遵循 API → Service → Repository/Text2SQL 模块边界。
- 前端通过统一 API Client 访问后端。
- LLM 不直接访问数据库。
- 模型 SQL 只能通过 SQL Validator 和只读 Executor 执行。
- `app` Schema 不得暴露给 Text2SQL 查询账号。

## 4. 安全规则

- 禁止提交真实 API Key、密码、连接串和 Token。
- 禁止在前端、浏览器存储、响应体或普通日志中保存模型密钥。
- 用户、模型和数据库内容均视为不可信输入。
- 禁止执行未经 AST 安全校验的模型 SQL。
- 禁止用固定问题匹配固定答案冒充 Text2SQL。
- 禁止静默忽略异常；对外错误必须脱敏并包含 Request ID。
- 禁止直接渲染未经清洗的 HTML。

## 5. 代码质量

- 每个模块只承担一个明确职责。
- 函数保持短小；复杂逻辑拆成可测试的纯函数或服务。
- 使用明确名称，不使用无意义缩写。
- 不新增未使用代码、注释掉的旧实现或无对应需求的抽象层。
- 公共行为优先抽取，避免复制粘贴。
- 注释解释“为什么”，不重复代码字面含义。
- 生成代码必须可重新生成，不手工修改生成产物。

## 6. 测试与完成标准

每个任务必须：

- 增加与风险相称的测试。
- 运行受影响模块的 Lint、类型检查和测试。
- 涉及构建配置时运行生产构建。
- 涉及数据库时验证迁移和固定 Seed。
- 涉及 API 时同步 OpenAPI 和前端类型。
- 报告实际运行的命令与结果，不声称未运行的测试通过。

未通过测试、类型检查或构建的任务不能标记完成。

## 7. Git 与变更保护

- 保留用户已有修改，不重置或覆盖无关变更。
- 一个提交表达一个意图。
- 提交遵循 Conventional Commits。
- 不提交 `.env`、数据卷、构建产物、运行日志和缓存。
- 数据库破坏性迁移必须显式说明风险与回滚方式。

## 8. AI Agent 工作方式

Agent 接到任务后应：

1. 检查目标目录、文档和现有修改。
2. 复述任务边界并给出简短计划。
3. 小步实现，不扩展范围。
4. 运行验证。
5. 自查安全、错误处理和边界状态。
6. 最后报告修改文件、验证结果、风险和遗留项。

## 9. 沟通与状态反馈

- 每个 Agent 回合必须产生非空的用户可见回复；禁止以空字符串、空 Markdown 或仅工具调用结束。
- 收到跨任务协调指令后，先用一句简短 commentary 确认“已收到”和当前动作。
- 回合结束必须提供非空 final；即使没有代码变化，也要明确报告“暂无新变化”、当前状态和下一步。
- 长任务至少每 60 秒提供一次可验证的进度反馈，不重复发送无信息量的状态。
- 协调任务派发消息后必须检查目标任务是否产生非空回复。目标任务完成但回复为空时，视为沟通失败：重试一次明确的最小指令；仍为空则由协调任务直接向用户报告实际状态。
- 不得把任务处于 `active`、收到消息或完成空回合作为“正在正常推进”的证据；必须结合文件变化、命令结果或非空状态说明判断。
- 自动巡检即使没有进展，也必须回复“暂无新变化”，不得静默或输出空消息。
